export default {
    salesEmployees(state) {
      return state.salesEmployees;
    },
    // findProduct(state) {
    //   return id => state.products.filter(user => user._id === id);
    // },
  };
  